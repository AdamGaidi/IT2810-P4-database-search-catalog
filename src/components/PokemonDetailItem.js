import React from "react";
import Card from "components/Card";
import "./PokemonDetailItem.css";

const PokemonDetailItem = ({ src, stars, hasStarred, name, types }) => {
  return (
    <Card>
      hello
      {name}
    </Card>
  );
};

export default PokemonDetailItem;

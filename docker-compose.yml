version: '3.3'

services:
   db:
     image: mysql:latest
     # volumes:
     #   - db_data:/var/lib/mysql
     restart: always
     environment:
       MYSQL_ROOT_PASSWORD: root_password
       MYSQL_DATABASE: pokedex
       MYSQL_USER: username
       MYSQL_PASSWORD: user_password

# Uncomment when out of development phase of db
# volumes:
#     db_data:
